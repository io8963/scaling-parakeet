---
title: Markdown 样式测试文档 - All Features
date: 2025-12-03
summary: 包含所有常用 Markdown 元素，用于测试博客的渲染和样式兼容性。
tags: [测试, Markdown, 样式, 代码]
---

# H1 - 顶级标题 (文章标题)

## H2 - 布局与结构测试

这是文章的第一个段落。Markdown 应该能够清晰地展示各种文本样式。

我们来测试一下：
**加粗文本 (Strong)**，*斜体文本 (Emphasis)*，~~删除线文本~~。
**_加粗斜体_**。
段落中的 `行内代码` 应该有独立的背景色和字体样式。

### H3 - 列表与链接

#### H4 - 标题层级展示

##### H5 - 小标题

###### H6 - 最小标题

---

这是一条水平分隔线，用于分隔内容区域。

### 列表 (Lists)

**无序列表：**

* 列表项一
    * 嵌套列表 A
    * 嵌套列表 B：请注意列表标记（如圆点）是否着色。
* 列表项二
* 列表项三

**有序列表：**

1. 第一步：初始化项目
2. 第二步：安装依赖
    1. 子步骤 2.1
    2. 子步骤 2.2：检查编号是否正确。
3. 第三步：运行脚本

### 链接与图片

这是一个 [普通内联链接](https://www.google.com)。

这是一个 [外部链接，应带有“↗”标记](https://github.com/io8963){:target="_blank"}，检查它是否在新标签页打开。

这是图片占位符（请确保 `media/placeholder-image.jpg` 文件存在）：
![占位图描述：用于测试图片显示和圆角](media/placeholder-image.jpg)

### 引用块 (Blockquotes)

> 这是一个标准的引用块。
> 它应有一个垂直的边线和不同的背景色，以突出强调。
> **我们在这里测试强调文本**，以及 `行内代码` 在引用块中的表现。

## 📝 技术内容测试：代码与表格

### 代码展示 (Code Blocks)

**Python 代码块 (Codehilite 高亮测试):**

```python
# 这是一个 Python 代码块，用于测试代码高亮主题
import os
from datetime import datetime

def build_post(post_data: dict):
    """处理并生成单篇文章"""
    title = post_data.get('title', 'Untitled')
    date_str = post_data['date'].strftime('%Y-%m-%d')
    
    # 打印调试信息
    print(f"Building: {title} ({date_str})")
    
    # 返回最终 HTML
    return f"<h1>{title}</h1>"

# 运行主函数
if __name__ == '__main__':
    # 这是一个长注释，用来测试代码块的横向滚动（如果代码很宽）
    print("Generation finished successfully.")


**CSS 代码块:**

/* 这是一个 CSS 代码块，测试 CSS 语法高亮 */
.header {
    position: sticky;
    top: 0;
    /* Glassmorphism 效果 */
    backdrop-filter: blur(5px);
    background-color: rgba(255, 255, 255, 0.95);
    z-index: 1000;
    border-radius: 0 0 12px 12px; /* 检查圆角是否生效 */
}

@media (max-width: 768px) {
    .site-title {
        font-size: 1.3em;
    }
}


### 表格 (Tables)

这是一个标准的 Markdown 表格，应带有斑马线效果、边框和圆角。

| 标题 | 描述 | 状态 | 优势 |
| :--- | :--- | :---: | ---: |
| **项目** | **功能** | **结果** | **价值** |
| 性能 | 纯静态生成 | ✅ | 极速加载 |
| 适配 | 自动暗色模式 | ✅ | 保护视力 |
| 维护 | 依赖极少 | ⏳ | 长期稳定 |
| 样式 | 移动端优化 | ⚠️ | 待优化 |



## 🧪 其他排版测试

### 特殊字符和 HTML 实体

版权符号 &copy;，注册商标 &reg;。
测试中文标点符号：句号，逗号、引号“”，书名号《》。

### 强调文本测试

这是**一段包含粗体**的文字。
这是*一段包含斜体*的文字。
这是 *被强调的文本*，应该与普通文本有所区别，例如变色。




